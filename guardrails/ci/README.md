# CI Guardrails

These scripts are simple guardrails that can be used in CI to fail fast on unsafe configurations.

Suggested checks:
- Helm lint
- YAML lint
- Required probes
- Required requests/limits
- Approved registries only
